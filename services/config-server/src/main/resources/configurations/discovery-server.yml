eureka:
  instance:
    hostname: localhost
  server:
    port: 8761
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone:
        "http://${eureka.instance.hostname}/${server.port}/eureka/"