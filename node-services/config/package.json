{
  "name": "@gecommerce/config",
  "version": "1.0.0",
  "description": "\"Schemas and types for gecommerce as a common sharable package\"",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev:watch": "nodemon --watch 'src/**/*' -e ts,tsx --exec 'tsc'"
  },
  "author": "Beez dev",
  "license": "CUSTOM",
  "devDependencies": {
    "@types/node": "^24.1.0",
    "nodemon": "^3.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "dotenv": "^17.2.1"
  }
}
